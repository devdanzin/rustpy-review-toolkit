{
  "$schema": "https://anthropic.com/claude-code/marketplace.schema.json",
  "name": "rustpy-review-toolkit",
  "version": "0.1.0-dev",
  "description": "Review toolkit for the RustPython interpreter itself — a Python runtime written in Rust. Runtime-implementer perspective: it audits RustPython's own soundness, not extensions built with it. 6 specialized agents covering Python-reachable panic sites (unwrap/expect/index tiered by protocol-trait reachability), unsafe layout-transmute and pointer-cast inconsistency in the object model, GC Traverse-completeness for the cycle collector, complexity, and git history. RustPython shares PyO3's vocabulary (PyRef, PyResult, #[pyclass]) but not its semantics, so the checks are RustPython-native, not transplanted from the PyO3 toolkits. Tree-sitter-rust-powered, static-first.",
  "owner": {
    "name": "Charlie Lin and Danzin",
    "email": "tuug@gmx.us"
  },
  "plugins": [
    {
      "name": "rustpy-review-toolkit",
      "description": "6 specialized agents and 4 commands for auditing the RustPython interpreter's own Rust source. Tree-sitter-rust-powered, static-first. Flagship: Python-reachable panic sites (.unwrap()/.expect()/panic!/index) ranked by reachability tier (py > protocol > internal) so runtime panics that a Python program can trigger surface while internal invariants stay silent. Plus unsafe-soundness (the PyAtomicRef cross-method pointer-cast inconsistency / handle-transmute-without-guard class), GC Traverse-completeness (owned-ref fields with no traverse opt-in → uncollectable cycles), complexity, and git history. Includes a `known-issues` command that cross-references a catalog of fuzzer-confirmed panic sites against a fresh scan to report which are still unfixed.",
      "source": "./plugins/rustpy-review-toolkit",
      "version": "0.1.0-dev",
      "category": "code-quality",
      "keywords": [
        "rustpython",
        "rust",
        "python-interpreter",
        "soundness",
        "panic-safety",
        "unsafe",
        "transmute",
        "garbage-collection",
        "traverse",
        "free-threading",
        "tree-sitter",
        "code-review"
      ]
    }
  ]
}
